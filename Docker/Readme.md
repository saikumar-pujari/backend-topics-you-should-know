### Docker is basically a container.
where we can add our required all the files in one place so that it can be tested in same env in other computer.

> Docker is basically building a file which can be runned in any enviorment with the file without any dependes from the system

 >the most important problem was using same port number in multiple project.

creating a VM(Virtual Machines) is a alternative for this but when we use the VM it will use the RAM and storage which is decidated from the orignal server which may restrict there actuall performace rather the docker is build on a host where it uses the host shared resources it will manage them interally

```
Docker can internally implement the Load balancer for ngnix with the servers in them using the resolver or network between them
```

